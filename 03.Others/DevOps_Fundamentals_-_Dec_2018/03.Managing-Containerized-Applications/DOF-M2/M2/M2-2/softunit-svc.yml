apiVersion: v1
kind: Service
metadata:
  name: softunit-svc
  labels:
    app: softunit
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 30001
    protocol: TCP
  selector:
    app: softunit
